namespace Client.Presentation;

/// <summary>
/// Root component: shows a loading spinner until the session is initialized, then either the
/// <c>Login</c> screen or the routed app. Bootstraps prefs/theme/session and handles 401 sign-out.
/// </summary>
public partial class App;
