# Capitrack Environment Configuration
# Copy this file to .env and update the values (keep the .env at root)

# Server Configuration
PORT=3000
SESSION_SECRET=your-secure-session-secret-here

# Database Configuration
DB_PATH=./data/capitrack.db

# Initial User Configuration (only used on first run when database is empty)
CAPITRACK_INIT_USERNAME=admin
CAPITRACK_INIT_PASSWORD=change-me-immediately
CAPITRACK_BASE_CURRENCY=USD
